/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
/**
 * Canonicalizes a connector ID to the APIM "shared" form.
 * `"office365"` → `"shared_office365"`; `"shared_office365"` stays unchanged.
 */
export function normalizeConnectorId(connectorId) {
    return connectorId.startsWith('shared_') ? connectorId : `shared_${connectorId}`;
}
/**
 * Returns `true` when a connector ID refers to the SQL connector, ignoring the
 * optional `shared_` prefix and case (`"sql"`, `"shared_sql"`, `"SQL"` all match).
 */
export function isSqlConnector(connectorId) {
    return connectorId.replace('shared_', '').toLowerCase() === 'sql';
}
//# sourceMappingURL=ConnectorId.js.map