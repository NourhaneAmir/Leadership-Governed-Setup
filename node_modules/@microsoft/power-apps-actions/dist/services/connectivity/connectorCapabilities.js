/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
/**
 * Returns true when the connector's `capabilities` array advertises the
 * "tabular" capability (case-insensitive). Tabular connectors expose tables
 * (e.g., SQL, SharePoint, Oracle, MySQL, PostgreSQL) and require a
 * `resourceName`/`dataset` when adding as a data source.
 */
export function hasTabularCapability(capabilities) {
    return capabilities?.some((c) => c.toLowerCase() === 'tabular') ?? false;
}
//# sourceMappingURL=connectorCapabilities.js.map