/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import { ConfidentialClientApplication } from '@azure/msal-node';
import { getAuthority } from '../CliUtils.js';
import { AuthenticationError, ConfigurationError } from '../Errors/CliError.js';
import { cliEnvVarFullName, getCliEnvVar } from '../Utils/EnvVars.js';
/**
 * Authenticates using a service principal (client credentials flow). Activated with
 * PA_CLI_USE_SP_AUTH=true or CI=true.
 */
export class ServicePrincipalAuthenticationProvider {
    _msalClient;
    _clientId;
    _clientSecret;
    _tenantId;
    _region;
    constructor() {
        const clientId = getCliEnvVar('SP_CLIENT_ID');
        const clientSecret = getCliEnvVar('SP_CLIENT_SECRET');
        const tenantId = getCliEnvVar('SP_TENANT_ID');
        if (!clientId || !clientSecret || !tenantId) {
            throw new ConfigurationError(`Service Principal environment variables ${cliEnvVarFullName('SP_CLIENT_ID')}, ${cliEnvVarFullName('SP_CLIENT_SECRET')}, and ${cliEnvVarFullName('SP_TENANT_ID')} must be set.`, `Set ${cliEnvVarFullName('SP_CLIENT_ID')}, ${cliEnvVarFullName('SP_CLIENT_SECRET')}, and ${cliEnvVarFullName('SP_TENANT_ID')} before running the CLI in service-principal mode.`);
        }
        this._clientId = clientId;
        this._clientSecret = clientSecret;
        this._tenantId = tenantId;
    }
    async initAsync(region) {
        this._region = region;
        const authConfig = {
            auth: {
                clientId: this._clientId,
                clientSecret: this._clientSecret,
                authority: getAuthority(this._region, this._tenantId),
            },
        };
        this._msalClient = new ConfidentialClientApplication(authConfig);
    }
    async getAccessTokenForResource(resource) {
        if (!this._msalClient) {
            throw new Error('Authentication not initialized yet');
        }
        const clientCredentialRequest = {
            scopes: [resource + '/.default'],
        };
        const result = await this._msalClient.acquireTokenByClientCredential(clientCredentialRequest);
        if (!result?.accessToken) {
            throw new AuthenticationError('Failed to acquire token using service principal.');
        }
        return result.accessToken;
    }
    getUserTenantId() {
        return this._tenantId;
    }
    async getCachedAccountAsync() {
        // No interactive account, but a client-credentials token is always obtainable without a browser,
        // so report a stable identifier. This lets telemetry eagerly fetch environment metadata in CI.
        return `ServicePrincipal:${this._clientId}`;
    }
}
//# sourceMappingURL=ServicePrincipalAuthenticationProvider.js.map