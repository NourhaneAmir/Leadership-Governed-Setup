/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import type { CliEnvVarName } from '../Utils/EnvVars.js';
export type DefaultStringOption = CliOption & {
    message: string;
    initialValue?: string;
};
export type Flags = {
    key: string;
    alias?: string;
};
export interface CliOption<T = unknown> {
    flags: Flags;
    /**
     * Optional branded env-var fallback (short name; the `PA_CLI_` prefix is applied centrally by
     * {@link cliEnvVarFullName}). When set, ArgumentProvider.getOption() reads the prefixed env var
     * if no flag value was supplied. Omit for flags that intentionally have no env-var override.
     */
    env?: CliEnvVarName;
    description?: string;
    default?: T;
    customPrompt?: () => Promise<T>;
    optionalInNonInteractive?: boolean;
}
//# sourceMappingURL=Argument.types.d.ts.map