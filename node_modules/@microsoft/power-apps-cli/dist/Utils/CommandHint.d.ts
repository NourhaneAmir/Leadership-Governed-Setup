/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
/**
 * True when the CLI is running as the grouped `pa` binary. Falls back to `false`
 * (the frozen flat `power-apps` surface) when settings are unavailable, so shared
 * infrastructure never accidentally emits `pa ...` guidance on the flat binary.
 */
export declare function isGroupedCli(): boolean;
/**
 * Picks the command to reference in a user-facing hint or remediation string for the
 * invoked binary: the grouped `pa ...` form under the `pa` binary, otherwise the flat
 * `npx power-apps ...` form under the frozen `power-apps` binary. Verbs pass their
 * `argumentProvider.isGrouped()`; shared infrastructure omits the argument and reads
 * the mode from settings.
 */
export declare function surfaceCommand(grouped: string, flat: string, grouped_?: boolean): string;
//# sourceMappingURL=CommandHint.d.ts.map