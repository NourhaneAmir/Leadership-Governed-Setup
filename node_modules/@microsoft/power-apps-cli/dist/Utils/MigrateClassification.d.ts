/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
export type MigratableAppType = 'vibe' | 'appBuilder';
export interface ConnectorDataSource {
    /** Connector reference handled by the generic code-app to MAAF migration path. */
    connectionReferenceKey: string;
    apiId: string;
    displayName?: string;
}
export interface DataverseTableDataSource {
    /** Dataverse table reference salvaged on a best-effort basis for Vibe apps. */
    logicalName: string;
    entitySetName?: string;
}
export interface DataTopology {
    connectors: ConnectorDataSource[];
    dataverseTables: DataverseTableDataSource[];
    hasDataverse: boolean;
    dataverseTableCount: number;
    hasConnectors: boolean;
    connectorCount: number;
}
export interface AppClassificationResult {
    appType: MigratableAppType;
    sourceRoot: string;
    powerConfigPath: string;
    dataTopology: DataTopology;
}
/**
 * Connector apiId of the Microsoft MCP Servers connector. App Builder apps carry it but never use it
 * in the migrated MAAF app, so it is dropped end-to-end (schema not ported, connector not
 * re-codegenned). Used for topology / apiId filtering.
 */
export declare const MCP_CONNECTOR_API_ID = "shared_a365mcpservers";
/**
 * The MCP connector's `.power/` schema folder + `dataSourcesInfo` key, which drop the `shared_`
 * prefix. Used to skip the schema relocation in step 4.
 */
export declare const MCP_CONNECTOR_SCHEMA_KEY = "a365mcpservers";
export declare function classifySourceApp(cwd?: string): AppClassificationResult;
export declare function getLastPathSegment(value: string): string;
//# sourceMappingURL=MigrateClassification.d.ts.map