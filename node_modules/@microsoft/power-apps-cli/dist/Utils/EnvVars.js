/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
/**
 * Every environment variable the Power Apps CLI defines is namespaced with the `PA_CLI_`
 * prefix. This module is the single place that touches `process.env` for branded variables;
 * no code outside of it should build the prefixed name by hand.
 */
const PREFIX = 'PA_CLI_';
/** Returns the fully-qualified `PA_CLI_*` name for a branded CLI env var. */
export function cliEnvVarFullName(name) {
    return `${PREFIX}${name}`;
}
/** Read a branded `PA_CLI_*` env var by its short name. */
export function getCliEnvVar(name) {
    return process.env[cliEnvVarFullName(name)];
}
/** Read a branded `PA_CLI_*` env var as a boolean (`true` only when the value is exactly `'true'`). */
export function getCliEnvVarBool(name) {
    return getCliEnvVar(name) === 'true';
}
/** Set a branded `PA_CLI_*` env var by its short name. */
export function setCliEnvVar(name, value) {
    process.env[cliEnvVarFullName(name)] = value;
}
/** Delete a branded `PA_CLI_*` env var by its short name. */
export function deleteCliEnvVar(name) {
    delete process.env[cliEnvVarFullName(name)];
}
/** Read a system or external env var that has no `PA_CLI_` prefix (e.g. CI, NO_COLOR, LOCALAPPDATA). */
export function getSystemEnvVar(name) {
    return process.env[name];
}
/** Set a system env var that has no `PA_CLI_` prefix (e.g. NO_COLOR). */
export function setSystemEnvVar(name, value) {
    process.env[name] = value;
}
/** Delete a system env var that has no `PA_CLI_` prefix. */
export function deleteSystemEnvVar(name) {
    delete process.env[name];
}
//# sourceMappingURL=EnvVars.js.map