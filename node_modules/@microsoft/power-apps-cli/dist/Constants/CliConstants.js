/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
/**
 * Binary name for the new grouped CLI surface, used in help text and error messages.
 *
 * The existing flat surface ships as the frozen `power-apps` binary (that name is hardcoded in the
 * legacy paths). The new declarative, grouped surface ships as `pa`. At 1.x the `power-apps` binary
 * is removed and `pa` becomes the sole CLI.
 */
export const PA_BINARY_NAME = 'pa';
//# sourceMappingURL=CliConstants.js.map