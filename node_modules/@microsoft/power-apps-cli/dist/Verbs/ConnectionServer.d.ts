/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import type { Region } from '@microsoft/power-apps-common/services';
export type ConnectionServerConfig = {
    connectorName: string;
    environmentId: string;
    region: Region;
    /**
     * How long to wait for the browser to post back before giving up. Defaults to
     * {@link DEFAULT_CONNECTION_CREATION_TIMEOUT_MS} (10 minutes). Tests pass a
     * short value so the flow fails fast instead of hanging.
     */
    timeoutMs?: number;
};
export type ConnectionResult = {
    status: 'created' | 'cancelled' | 'error';
    connectionId?: string;
    connectionName?: string;
    displayName?: string;
    errorMessage?: string;
};
/**
 * Starts a local HTTP server, opens the maaf-player connection page in the
 * browser, and waits for a callback with the result. Uses the same pattern as
 * an OAuth authorization code flow: nonce for CSRF, protocol version for drift
 * detection, and a timeout for cleanup.
 */
export declare function startConnectionServerAsync(config: ConnectionServerConfig): Promise<ConnectionResult>;
//# sourceMappingURL=ConnectionServer.d.ts.map