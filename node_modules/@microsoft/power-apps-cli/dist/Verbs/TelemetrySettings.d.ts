/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import type { ArgumentProvider } from '../ArgumentProvider.js';
/**
 * Modify the user telemetry settings
 * Does not resolve conflicts between options; Just writes whatever is provided
 * @param {ArgumentProvider} argumentProvider
 * @returns {Promise<void>}
 */
export declare function modifyTelemetrySettings(argumentProvider: ArgumentProvider): Promise<void>;
/**
 * Grouped-surface handler for `pa telemetry enable`. Turns telemetry on, honoring the optional
 * console-output flags, then leaves the rest of the settings untouched.
 */
export declare function telemetryEnableAsync(argumentProvider: ArgumentProvider): Promise<void>;
/**
 * Grouped-surface handler for `pa telemetry disable`. Turns telemetry off, honoring the optional
 * console-output flags, then leaves the rest of the settings untouched.
 */
export declare function telemetryDisableAsync(argumentProvider: ArgumentProvider): Promise<void>;
/**
 * Grouped-surface handler for `pa telemetry status`. Displays the current telemetry settings.
 */
export declare function telemetryStatusAsync(argumentProvider: ArgumentProvider): Promise<void>;
//# sourceMappingURL=TelemetrySettings.d.ts.map