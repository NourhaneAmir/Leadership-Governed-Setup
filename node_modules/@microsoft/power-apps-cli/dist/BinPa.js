#!/usr/bin/env node
/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import { boot } from './Cli.js';
// Entry point for the new grouped `pa` surface. Delegates to the shared boot with the grouped mode,
// which dispatches through the declarative command registry. The frozen `power-apps` binary uses
// Bin.ts (flat mode). Both entry files share all setup and handler logic in Cli.ts.
boot('grouped');
//# sourceMappingURL=BinPa.js.map